export interface Theme {
  fonts: {
    body: string,
    heading: string,
    monospace: string
  },
  colors: {
    text: string,
    background: string,
    primary: string
    secondary: string
  }
}